use_frameworks!

target 'WebSocketChat' do
    pod 'Socket.IO-Client-Swift', '~> 8.0.2' # Or latest version
    pod 'Gloss', '~> 1.0'
    pod 'Cosmos', '~> 6.0'

    target 'WebSocketChatTests' do
        inherit! :search_paths
    end
end

